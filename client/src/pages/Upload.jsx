import React from "react";
import UploadNote from "../components/UploadNotes";

const Upload = () => {
    return (
        <div className="h-heightWithoutNavbar flex items-center justify-center">
            <UploadNote />
        </div>
    );
};

export default Upload;